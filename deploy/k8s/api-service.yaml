apiVersion: v1
kind: Service
metadata:
  name: ecom-api
  namespace: ecom
spec:
  type: NodePort
  selector:
    app: ecom-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
